<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Pomodojo"
        tools:targetApi="31">

        <!-- Existing activities -->
        <activity
            android:name=".functionality.dashboard.ui.HomeActivity"
            android:exported="false" />
        <activity
            android:name=".functionality.auth.ui.SignUpActivity"
            android:exported="false" />
        <activity
            android:name=".functionality.auth.ui.FollowUpActivity"
            android:exported="false" />
        <activity
            android:name=".functionality.auth.ui.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".functionality.pomodoro.ui.ShortBreakActivity" />
        <activity
            android:name=".functionality.pomodoro.ui.WorkTimeActivity"
            android:exported="false" />

        <!-- New activities -->
        <activity
            android:name=".functionality.spotify.SpotifyPlaylist"
            android:exported="false" />
        <activity
            android:name=".functionality.facescan.FaceScan"
            android:exported="false" />

        <service
            android:name=".functionality.pomodoro.service.TimerService"
            android:exported="false"
            android:foregroundServiceType="specialUse" />

    </application>

</manifest>